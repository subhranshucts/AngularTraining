import { DirectoryDirective } from './directory.directive';

describe('DirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
